# 架构说明（简要）

- 前端：使用 WebAudio API 实现低延迟播放、键盘映射与可视化（瀑布流、按键高亮）。
- 后端：Python Flask 暴露转谱接口；复杂转谱可放在后端并调用 Magenta、Basic-Pitch 等模型。
- 数据流：音频文件上传 -> 后端处理（预处理 + 模型推断）-> 返回结构化 MIDI JSON -> 前端调度并渲染。

下一步建议：将转谱模型部署为异步任务，返回任务 id，前端轮询或通过 WebSocket 获取结果。
